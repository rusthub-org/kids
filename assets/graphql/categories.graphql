query CategoriesData {
  categories {
    id
    nameZh
    nameEn
    namePlural
    slug
  }
}

query CategoryBySlugData(
  $slug: String!
) {
  categoryBySlug(
    slug: $slug
  ) {
    id
    nameZh
    nameEn
    namePlural
    slug
  }
}
