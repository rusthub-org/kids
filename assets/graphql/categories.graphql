query CategoriesData {
  categories {
    id
    nameZh
    nameEn
  }
}

query CategoryBySlugData(
  $slug: String!
) {
  categoryBySlug(
    slug: $slug
  ) {
    id
    nameZh
    nameEn
    slug
  }
}
