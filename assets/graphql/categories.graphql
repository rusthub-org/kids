query CategoriesData {
  categories {
    id
    nameZh
    nameEn
    slug
  }
}

query CategoryBySlugData(
  $slug: String!
) {
  categoryBySlug(
    slug: $slug
  ) {
    id
    nameZh
    nameEn
    slug
  }
}
